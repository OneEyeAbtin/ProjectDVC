# Plan 4: Minecraft Reattach
Goal: port legacy mc_brain.py + mc_tasks.py responsibilities into src/main/services/minecraft.service.js; drone/ (bot.js + bots/) stays UNTOUCHED.
Architecture: main spawns `node drone/bot.js <args>` child; WS CLIENT connects to bot's ws server (ws://localhost:<ws_port>); events → bus (mc:* topics) → IPC pushes (mcEvent channels); commands: raw % text passthrough (bot has own queue — legacy Python queue was bypassed, do NOT port TaskQueue); #chat/whispers → MC brain LLM (separate url/key/model config, JSON {chat,emotion} response, ported MC_SYSTEM prompt + local response pools from legacy mc_brain.py _R).
Tasks: A) minecraft.service (lifecycle, WS client with reconnect, event router: observation/task_result/chat/error/radar/status_update/inventory_update/whisper, local pools, MC brain LLM, % passthrough, settings keys minecraft_v2) + tests (WS mocked). B) Renderer: MC settings tab (host/port/user/version/auth/ws_port + brain url/key/model + ai toggles), ⛏ connect button in top bar (spinner while connecting), bottom tab switch (Chat | Console | Radar | Inv) — console log panel, radar canvas (32-block, ported drawing), inventory+status panel, % input in console. C) Final combined audit: security/stress/npm-module review of whole app + Plan 4 + docs.
Constraints: no new deps beyond `ws` (already transitive — promote to direct dependency); drone/ untouched; renderer sandbox intact; all tests stay green.
